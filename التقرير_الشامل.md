# التقرير الشامل لتحليل نظام "سكني" — المستوى التالي
**تاريخ التقرير:** 5 يونيو 2026  
**النظام:** نظام إدارة السكن الطلابي (Node.js/Express + React 19 + SQL Server + Tailwind 4)

---

## فِهْرس المحتويات
1. [ملخص تنفيذي](#1-ملخص-تنفيذي)
2. [الأخطاء الحرجة (Critical)](#2-الأخطاء-الحرجة-critical)
3. [الأخطاء عالية الخطورة (High)](#3-الأخطاء-عالية-الخطورة-high)
4. [الأخطاء متوسطة الخطورة (Medium)](#4-الأخطاء-متوسطة-الخطورة-medium)
5. [الأخطاء منخفضة الخطورة (Low)](#5-الأخطاء-منخفضة-الخطورة-low)
6. [تحسينات البنية التحتية (Infrastructure)](#6-تحسينات-البنية-التحتية-infrastructure)
7. [تحسينات الأداء والأمان (Performance & Security)](#7-تحسينات-الأداء-والأمان-performance--security)
8. [جودة الكود (Code Quality)](#8-جودة-الكود-code-quality)
9. [التجربة التطويرية (DX) و DevOps](#9-التجربة-التطويرية-dx-و-devops)
10. [خطة الإصلاح المقترحة — بالترتيب](#10-خطة-الإصلاح-المقترحة--بالترتيب)

---

## 1. ملخص تنفيذي

**الوضع الحالي:** التطبيق في حالة جيدة بشكل عام. تم إنجاز:
- ✅ كل الثغرات الأمنية الحرجة (تأمين JWT، rate limiting، token blacklist)
- ✅ كل مشاكل قاعدة البيانات الحرجة (أعمدة attendance، notifications، delays)
- ✅ تأمين 19 endpoint في الراديو بـ `authorizePermission`
- ✅ إضافة `can_view_radio` وتوحيد نظام الصلاحيات
- ✅ 36+ route عليها Zod validation
- ✅ RBAC seeding + custom_permissions
- ✅ CI pipeline + Docker + PWA + Playwright E2E

**ما زال يحتاج للارتقاء للمستوى التالي — 36 مشكلة موزعة كالتالي:**

| المستوى | العدد | الوصف |
|---------|-------|-------|
| **حرج (Critical)** | 3 | يسبب أعطال أو فشل في النشر |
| **عالٍ (High)** | 8 | ثغرات أمنية ومشاكل أداء حقيقية |
| **متوسط (Medium)** | 15 | تحسينات هيكلية وجودة |
| **منخفض (Low)** | 10 | تحسينات خبرة المطور وتنظيف |

---

## 2. الأخطاء الحرجة (Critical)

### 🔴 CRIT-1: ملفات God/Single Responsibility
**الوصف:** 4 ملفات تتجاوز 2000 سطر والمئات منها تتجاوز 500 سطر:
- `EventsPage.tsx` — 2951 سطر
- `StudentsPage.tsx` — 2308 سطر  
- `AdminRadio514.tsx` — 2232 سطر
- `Radio.tsx` — 2185 سطر
- `radio.routes.ts` — 1607 سطر
- `event.routes.ts` — 1335 سطر
- `db.ts` — 974 سطر (schema + seed + audit + permissions)
- `server.ts` — 320 سطر (HTTP + Socket.io + Vite + rate limit + upload cleanup)

**التأثير:** استحالة الصيانة، سباق الـ merge conflicts، صعوبة الاختبار  
**الإصلاح:** تقسيم كل ملف >500 سطر إلى وحدات منطقية (controllers, services, components)

### 🔴 CRIT-2: No Rate Limiting على /api/radio/* بالكامل
**الملف:** `server.ts:184`  
```typescript
skip: (req) => isTest || req.path.startsWith('/api/radio/')
```
**الوصف:** كل endpoints الراديو بدون rate limit — أي مستخدم يضغط API آلاف المرات  
**التأثير:** ثغرة DoS حقيقية — يمكن استهداف upload-image أو chat/send  
**الإصلاح:** إزالة `/api/radio/` من قائمة الـ skip وإضافة rate limits خاصة للراديو

### 🔴 CRIT-3: Dockerfile لا ينسخ knexfile.ts (و ملفات .ts)
**الملف:** `Dockerfile`  
**الوصف:** الـ `RUN npx tsc --noEmit` يحتاج `knexfile.ts` في الصورة، لكن الـ `builder` stage يشتغل من `/app` والملف موجود. لكن الـ `runner` stage ينسخ `./knexfile.ts` فقط — إذا كان التشغيل يعتمد على اكتمال الـ `dist/`، فقد لا يعمل الـ `migrate` وقت التشغيل  
**التأثير:** فشل الترحيلات التلقائية (auto-migration) عند النشر بالـ Docker  
**الإصلاح:** التأكد أن `knexfile.ts` + `migrations/` في المسار الصحيح بعد البناء، أو استخدام `tsx` بدل `node` في الـ CMD

---

## 3. الأخطاء عالية الخطورة (High)

### 🟠 HIGH-1: Content Security Policy (CSP) معطل بالكامل
**الملف:** `server.ts:161` — `contentSecurityPolicy: false`  
**الوصف:** helmet معمول لكن CSP مفعل. التطبيق يستخدم YouTube IFrame API، تحميل صور من مسارات مختلفة  
**التأثير:** خطر XSS — أي script يتم حقنه في محتوى مستخدم (تعليقات، رسائل) ينفذ في المتصفح  
**الإصلاح:** تفعيل CSP بالقواعد المناسبة:
```typescript
contentSecurityPolicy: {
  directives: {
    defaultSrc: ["'self'"],
    scriptSrc: ["'self'", "https://www.youtube.com", "https://cdn.jsdelivr.net"],
    imgSrc: ["'self'", "data:", "https://i.ytimg.com", "https://img.youtube.com"],
    // إلخ
  }
}
```

### 🟠 HIGH-2: User Enumeration في /api/auth/register
**الملف:** `auth.routes.ts:48`  
**الوصف:** الرسالة "البريد الإلكتروني مسجل من قبل. جرب تسجيل الدخول أو استخدم بريد آخر." تختلف عن رسالة الخطأ العادية  
**التأثير:** يمكن لأي مهاجم تخمين عناوين البريد الإلكتروني المسجلة  
**الإصلاح:** رسالة عامة "فشل التسجيل" في كل الحالات

### 🟠 HIGH-3: لا يوجد Validation على حجم الـ Request لكل Route
**الملف:** `server.ts:212` — `express.json({ limit: '2mb' })`  
**الوصف:** حد 2MB عام لجميع الـ JSON requests. بعض الـ routes مثل radio/chat/messages ممكن إرسال 2MB من النص  
**التأثير:** استهلاك الذاكرة، هجوم DoS عبر إرسال طلبات كبيرة  
**الإصلاح:** تحديد limits مختلفة لكل route حسب الحاجة (chat: 10KB, videos: 1MB, upload: 10MB)

### 🟠 HIGH-4: Audit Logger يسجل req.body كاملاً
**الملف:** `middleware.ts:157-160`  
**الوصف:** الفلتر يشيل `password`, `confirmPassword`, إلخ. لكن أي حقل sensitive تاني (رقم قومي، تليفون، عنوان) يتسجل في الـ audit log  
**الإصلاح:** إما تسجيل `{...filteredBody}` بعد إضافة حقول sensitivity، أو إزالة `body` من الـ audit بالكامل والاكتفاء بـ `action` و `status`

### 🟠 HIGH-5: checkUserPermission يضرب DB في كل Request
**الملف:** `db.ts:938-961`  
**الوصف:** `authorizePermission` ينادي `checkUserPermission` اللي يعمل `SELECT * FROM users` + ممكن `SELECT from tenant_custom_roles` + `SELECT from role_permissions`  
**التأثير:** حتى لو الـ user عنده الصلاحية، بنضرب 1-3 DB queries لكل request  
**الإصلاح:** 
1. Caching: حفظ user permissions في JWT token (payload) أو in-memory cache
2. Middleware يحسب الصلاحية مرة واحدة ويخزنها في `req.user.permissions`

### 🟠 HIGH-6: No CSRF Protection
**الوصف:** لا يوجد أي حماية CSRF  
**التأثير:** أي مستخدم يدخل على موقع خبيث وهو مسجل في سكني، الموقع الخبيث يقدر يعمل actions نيابة عنه  
**الإصلاح:** إما:
1. استخدام `csurf` middleware
2. استخدام SameSite=Strict على cookies (لكن التطبيق بيستخدم Bearer token مش cookies)
3. إضافة `X-CSRF-Token` header للطلبات الحساسة

### 🟠 HIGH-7: N+1 Queries + No Pagination
**الملف:** متعدد (dashboard.routes.ts, attendance.routes.ts, radio.routes.ts)  
**الوصف:**
- استعلامات داخل loops (N+1) في الـ dashboard
- `/api/radio/videos`, `/api/radio/chat/messages`, `/api/students` كلها بدون pagination
**التأثير:** مع 1000+ طالب أو 10000+ رسالة شات، الاستعلامات تعلق أو تاكل 100% CPU  
**الإصلاح:** 
1. Pagination `LIMIT/OFFSET` على كل list endpoints
2. Batch queries بدل N+1

### 🟠 HIGH-8: Radio endpoints عامة بعض الشيء
**الوصف:** بعض radio endpoints اللي المفروض تكون public (current-track, song-history like/unlike) مفتوحة لأي مستخدم مسجل. لكن `/api/radio/videos` (قائمة الفيديوهات) عليها `authenticate` فقط  
**التأثير:** أي مستخدم مسجل يشوف كل الفيديوهات (حتى الـ is_hidden)
**الإصلاح:** إضافة `is_active && !is_hidden` فلتر للقوائم العامة، أو `authorizePermission(VIEW_RADIO)` إذا القصد إنها للإدارة فقط

---

## 4. الأخطاء متوسطة الخطورة (Medium)

### 🟡 MED-1: فراغات هيكلية في TypeScript
**الملف:** `tsconfig.json`  
**الوصف:** الـ `strict: true` مش موجود، ولا `noUnusedLocals` ولا `noUnusedParameters`  
**التأثير:** الـ `as any` منتشر في كل مكان، errors متجاهلة وقت الـ build  
**الإصلاح:** تفعيل strict mode تدريجياً

### 🟡 MED-2: الاتصال بقاعدة البيانات مكرر
**الملف:** `mssql_db.ts` + `knex.ts`  
**الوصف:** في `mssql_db.ts` pool تاني مش مستخدم. `gracefulShutdown` في `server.ts` يحاول يقفل الاتنين  
**الإصلاح:** إزالة `mssql_db.ts` وكل reference له

### 🟡 MED-3: تعريف الجداول مكرر في مكانين
**الملف:** `schema_mssql.sql` + `db.ts`  
**الوصف:** نفس الجداول معمول `CREATE TABLE IF NOT EXISTS` في مكانين بتعاريف مختلفة  
**التأثير:** صعوبة تتبع الـ schema، خطر اختلاف التعريفات  
**الإصلاح:** توحيد كل التعريفات في `schema_mssql.sql` والـ `db.ts` يعمل only migration-like ALTERs

### 🟡 MED-4: 25 Scripts مبعثرة في /scripts
**الوصف:** `scripts/` فيه 25 ملف بين test و utility و seed و migrate، كتير منها مش منظم  
**الإصلاح:** تنظيم في مجلدات فرعية: `scripts/tests/`, `scripts/migrations/`, `scripts/utils/` — وحذف القديم

### 🟡 MED-5: لا يوجد Validation لرفع الملفات (Magic Bytes)
**الملف:** `middleware/upload.ts`  
**الوصف:** `fileFilter` بيشتغل على extension فقط. الملف `malware.exe` بعد تغيير اسمه إلى `photo.jpg` هيتقبل  
**الإصلاح:** إضافة التحقق من magic bytes (`file-type` package) بعد الاستلام

### 🟡 MED-6: Stale Closures في React Components
**الوصف:** نمط متكرر:
```tsx
const fetchRef = useRef(() => {});
fetchRef.current = fetchData;
useEffect(() => { fetchRef.current(); }, []); // stale closure
```
**الإصلاح:** إضافة dependencies صحيحة أو استخدام `useEffect` مع `useCallback` مباشرة

### 🟡 MED-7: عدم التحقق من isMounted في Async Operations
**الوصف:** `setState` بعد await في useEffect بدون isMounted
**الإصلاح:** إضافة `let mounted = true` + cleanup return لـ `mounted = false`

### 🟡 MED-8: Catch صامت يخفي الأخطاء
**الملفات:** متعدد — استخدام `catch {}` بدون أي log  
**الإصلاح:** `catch (err) { console.error(...) }` على الأقل

### 🟡 MED-9: Race Condition في البحث وتغيير التبويبات
**الوصف:** استخدام `AbortController` غير منتشر. تغيير الفلتر بسرعة يرسل طلبات متعددة  
**الإصلاح:** `AbortController` لكل fetch

### 🟡 MED-10: لا يوجد Lazy Loading للصور
**الوصف:** uploads و profile photos بتتحمل مباشرة  
**الإصلاح:** `loading="lazy"` على كل `<img>` + placeholder

### 🟡 MED-11: dateFormatColumn لا يدعم إلا MSSQL
**الملف:** `knex.ts:38-40`  
**الوصف:** الدالة بتستخدم `FORMAT()` اللي خاص بـ MSSQL  
**الإصلاح:** تنفيذ لكل DB client أو على الأقل توثيق القيد

### 🟡 MED-12: لا يوجد فيديوهات تعليمية أو Help System
**الوصف:** مفيش زر "مساعدة" أو شرح داخل التطبيق  
**الإصلاح:** إضافة Help Overlay لكل صفحة أساسية

### 🟡 MED-13: Keyboard Accessibility
**الوصف:** أغلب الـ interactive elements ملهاش `aria-*` attributes  
**الإصلاح:** إضافة `aria-label` على الأقل للأزرار اللي مش مكتوب عليها نص

### 🟡 MED-14: استخدام `any[]` بدل Types
**الوصف:** `useState<any[]>([])`, `useState<any>(null)` منتشر  
**الإصلاح:** تعريف interfaces لكل Type

### 🟡 MED-15: لا يوجد React Query (TanStack Query)
**الوصف:** كل fetch يدوي بـ `useState` + `useEffect` بدون caching أو retry أو stale management  
**الإصلاح:** تبني React Query لإدارة الـ server state

---

## 5. الأخطاء منخفضة الخطورة (Low)

### 🟢 LOW-1: window.location.reload() في AuthContext
**الملف:** `AuthContext.tsx:203` — `switchTenant`  
**الإصلاح:** `navigate(0)` أو `window.location.href` بدون `reload()`

### 🟢 LOW-2: لا يوجد import type للأنواع
**الوصف:** معظم `import { Type }` يمكن أن تكون `import type { Type }`  
**الإصلاح:** ESLint rule `@typescript-eslint/consistent-type-imports`

### 🟢 LOW-3: Side Effect أثناء Render
**الملف:** `CompetitionResultsPage.tsx` — `document.documentElement.setAttribute('dir', 'rtl')` أثناء render  
**الإصلاح:** نقل إلى `useEffect`

### 🟢 LOW-4: TRANSLATE ONCHAIN MIX-UP في attendance
**الوصف:** `return res.json(...)` داخل callback الترانزاكشن  
**الإصلاح:** نقل الاستجابة خارج الترانزاكشن

### 🟢 LOW-5: التعليقات العربية مشوهة
**الوصف:** UTF-8 encoding مشاكل في بعض الملفات  
**الإصلاح:** إعادة حفظ الملفات بـ UTF-8

### 🟢 LOW-6: لا يوجد .editorconfig
**الإصلاح:** إضافة `.editorconfig` لتوحيد indent/spaces

### 🟢 LOW-7: 20 ملف script في الجذر
**الإصلاح:** تنظيف scripts directory

### 🟢 LOW-8: لا يوجد CONTRIBUTING.md
**الإصلاح:** إضافة إرشادات المساهمة

### 🟢 LOW-9: لا يوجد CHANGELOG.md
**الإصلاح:** إضافة changelog

### 🟢 LOW-10: لا يوجد Swagger/OpenAPI Documentation
**الإصلاح:** إضافة توثيق API (ولو basic)

---

## 6. تحسينات البنية التحتية (Infrastructure)

### 🏗️ INFRA-1: لا يوجد Monitoring/Error Tracking
**الوصف:** لا Sentry ولا Datadog ولا أي Error tracking  
**التأثير:** الأخطاء في الإنتاج بتمر بدون علم الفريق  
**الإصلاح:** إضافة Sentry (مجاني)

### 🏗️ INFRA-2: No Structured Logging
**الوصف:** `logger.ts` يكتب text عادي، مش JSON  
**التأثير:** صعوبة البحث في logs، مش ممكن parse بالآلي  
**الإصلاح:** تحويل إلى JSON logging + log levels

### 🏗️ INFRA-3: No Database Backup Strategy
**الوصف:** مفيش باكアップ للـ database  
**الإصلاح:** إضافة MSSQL backup job أو script

### 🏗️ INFRA-4: Docker Compose ينقصه بعض الإعدادات
**الوصف:** 
- لا يوجد `restart: always` على DB
- لا يوجد healthcheck على DB
- كلمة مرور SA مثبتة في compose نفسها  
**الإصلاح:** إضافة healthcheck, restart, متغيرات البيئة

### 🏗️ INFRA-5: No Staging Environment
**الوصف:** CI يبني ويختبر على production config  
**الإصلاح:** إضافة staging deployment pipeline

---

## 7. تحسينات الأداء والأمان (Performance & Security)

### ⚡ PERF-1: الفهرسة (Indexes)
**الوصف:** الـ DB يكاد يكون بدون indexes غير unique constraints  
**التأثير:** `SELECT COUNT(*)` و `JOIN` على tables كبيرة (audit_logs, notifications, attendance) كارثة أداء  
**الإصلاح:** إضافة indexes:
```sql
-- الأهم أولاً:
CREATE INDEX IX_audit_logs_tenant_date ON audit_logs(tenant_id, created_at DESC);
CREATE INDEX IX_notifications_user_read ON notifications(user_id, is_read) INCLUDE(title);
CREATE INDEX IX_attendance_tenant_date ON attendance(tenant_id, created_at DESC);
CREATE INDEX IX_finances_tenant_date ON finances(tenant_id, date DESC);
CREATE INDEX IX_students_tenant_search ON students(tenant_id, status) INCLUDE(name);
CREATE INDEX IX_chat_messages_created ON radio_chat_messages(created_at DESC);
CREATE INDEX IX_radio_videos_active ON radio_videos(is_active, created_at DESC);
```

### ⚡ PERF-2: Frontend Bundle Size
**الوصف:** الـ bundle فيه `framer-motion` كلها حتى الصفحات البسيطة  
**التأثير:** first load بطيء  
**الإصلاح:** تفعيل code splitting لكل page + dynamic import لـ framer-motion

### ⚡ PERF-3: لا يوجد Caching Layer
**الوصف:** كل request يضرب DB  
**التأثير:** scalability محدودة  
**الإصلاح:** إضافة Redis أو in-memory cache للـ permissions والبيانات شبه الثابتة

### 🔒 SEC-1: إزالة /api/radio من Rate Limit Skip
**الوصف:** الموضح في CRIT-2  
**الإصلاح:** rate limit مخصص للراديو (upload: 10/min, chat: 30/min, videos: 60/min)

### 🔒 SEC-2: تفعيل CSP
**الوصف:** الموضح في HIGH-1

### 🔒 SEC-3: إضافة CSRF Protection
**الوصف:** الموضح في HIGH-6

### 🔒 SEC-4: تفعيل HTTPS في Production
**الوصف:** لا يوجد SSL/TLS تكوين في الـ Docker  
**الإصلاح:** إضافة reverse proxy (nginx/Caddy) مع Let's Encrypt

---

## 8. جودة الكود (Code Quality)

### 📐 CODE-1: تقسيم God Files
| الملف | الحجم | الإجراءات |
|-------|-------|-----------|
| `radio.routes.ts` | 1607 سطر | تقسيم إلى radio/controller.ts, radio/chat.ts, radio/stats.ts |
| `event.routes.ts` | 1335 سطر | فصل service layer عن routes |
| `db.ts` | 974 سطر | فصل إلى schema.ts, seed.ts, audit.ts, permissions.ts |
| `server.ts` | 320 سطر | فصل إلى socket.ts, middleware.ts, config.ts |
| `EventsPage.tsx` | 2951 سطر | تقسيم Components |
| `StudentsPage.tsx` | 2308 سطر | تقسيم Components |
| `AdminRadio514.tsx` | 2232 سطر | تقسيم Modules |
| `Radio.tsx` | 2185 سطر | تقسيم Modules |

### 📐 CODE-2: TypeScript Strict Mode
**الإجراء:** تفعيل تدريجي:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitReturns": true
  }
}
```

### 📐 CODE-3: React Component Patterns
**الإجراءات:**
- استخدام React Query للـ server state
- إضافة ErrorBoundary لكل feature module
- توحيد pattern الـ API calls (useApi متقن لكن ينقصه caching)
- إضافة useCallback + useMemo بشكل منهجي

### 📐 CODE-4: Controller-Service-Repository Pattern
**الإجراء:** فصل business logic من route handlers:
- Routes: validation + request parsing
- Services: business logic
- Repository: DB access

---

## 9. التجربة التطويرية (DX) و DevOps

### 🛠️ DX-1: Pre-commit Hooks
**الإجراء:** Husky + lint-staged لتشغيل `npm run lint` + `npx tsc --noEmit` قبل كل commit

### 🛠️ DX-2: API Documentation
**الإجراء:** إضافة OpenAPI/Swagger:
```typescript
// npm install swagger-ui-express swagger-jsdoc
import swaggerUi from 'swagger-ui-express';
import swaggerJsdoc from 'swagger-jsdoc';
```

### 🛠️ DX-3: Test Coverage
**الإجراء:**
- Unit tests: Vitest لكل service
- Integration tests: Supertest لكل route
- E2E: توسيع Playwright (حالياً 2 specs)
- Coverage target: 60%+ للـ services

### 🛠️ DX-4: Environment Validation
**الإجراء:** إضافة `env-schema` أو Zod validation لمتغيرات البيئة عند بدء التشغيل

### 🛠️ DX-5: Storybook
**الإجراء:** إضافة Storybook للمكونات المشتركة (Skeleton, Modal, ErrorBoundary, إلخ)

### 🛠️ DX-6: ESLint + Prettier
**الإجراء:**
- `eslint-plugin-react-hooks` (exhaustive-deps)
- `@typescript-eslint/no-explicit-any` مع Allow لبعض الأماكن
- Prettier للتنسيق التلقائي

---

## 10. خطة الإصلاح المقترحة — بالترتيب

### المرحلة الأولى — العاجلة (3-5 أيام)
| الأولوية | المشكلة | الجهد |
|----------|---------|-------|
| 1 | 🔴 CRIT-2: إزالة `/api/radio/` من skip rate limit | 1 ساعة |
| 2 | 🟠 HIGH-1: تفعيل CSP | 3 ساعات |
| 3 | 🟠 HIGH-5: Caching permissions | 4 ساعات |
| 4 | 🟠 HIGH-7: Pagination لنقاط الضعف | 6 ساعات |
| 5 | 🟠 HIGH-4: Audit logger يحذف sensitive data | 1 ساعة |

### المرحلة الثانية — المدى القصير (أسبوع)
| الأولوية | المشكلة | الجهد |
|----------|---------|-------|
| 6 | 🔴 CRIT-3: إصلاح Dockerfile | 2 ساعة |
| 7 | 🟠 HIGH-2: User enumeration fix | 30 دقيقة |
| 8 | 🟠 HIGH-3: Per-route body limits | 2 ساعة |
| 9 | 🟠 HIGH-6: CSRF protection | 3 ساعات |
| 10 | 🔴 CRIT-1: تقسيم أكبر God files | 3-5 أيام |
| 11 | 🟠 HIGH-8: فلترة video listings | 1 ساعة |
| 12 | ⚡ PERF-1: إضافة indexes (7 indexes) | 2 ساعة |

### المرحلة الثالثة — المدى البعيد (2-3 أسابيع)
| الأولوية | المشكلة | الجهد |
|----------|---------|-------|
| 13 | 🟡 MED-15: إضافة React Query | 5 أيام |
| 14 | 🟡 MED-1: TypeScript strict mode | 2-3 أيام |
| 15 | 🟡 MED-5: Magic bytes validation | 3 ساعات |
| 16 | 🟡 MED-6/7: Stale closures + isMounted | 4 ساعات |
| 17 | 🟡 MED-10: Lazy loading images | 2 ساعة |
| 18 | 🟡 MED-2: إزالة mssql_db.ts | 1 ساعة |
| 19 | 🟡 MED-3: توحيد الـ schema | 4 ساعات |
| 20 | 🟡 MED-4: تنظيم scripts | 2 ساعة |
| 21 | 🟡 MED-11: dateFormatColumn تعميم | 1 ساعة |

### المرحلة الرابعة — الاستثمارية (شهر+)
| الأولوية | المشكلة | الجهد |
|----------|---------|-------|
| 22 | 🏗️ INFRA-1: إضافة Sentry | 4 ساعات |
| 23 | 🏗️ INFRA-2: Structured JSON logging | 3 ساعات |
| 24 | 🏗️ INFRA-3: Database backup strategy | 4 ساعات |
| 25 | 🏗️ INFRA-4: Docker compose تحسينات | 2 ساعة |
| 26 | 🔒 SEC-4: HTTPS/SSL تكوين | 3 ساعات |
| 27 | 📐 CODE-4: Controller-Service pattern | 3-5 أيام |
| 28 | 🛠️ DX-1: Husky + pre-commit hooks | 2 ساعة |
| 29 | 🛠️ DX-2: OpenAPI doc | 2-3 أيام |
| 30 | 🛠️ DX-3: Test coverage expansion | 3-5 أيام |
| 31 | 🛠️ DX-5: Storybook | 3 أيام |

---

## الملخص العددي

| الفئة | العدد | الحالة |
|-------|-------|--------|
| 🔴 Critical | 3 | العناية الفورية |
| 🟠 High | 8 | يحتاج إجراء |
| 🟡 Medium | 15 | تحسين متوسط |
| 🟢 Low | 10 | تنظيف |
| 🏗️ Infrastructure | 5 | استثماري |
| ⚡ Performance | 3 | استثماري |
| 🔒 Security | 4 | أولوية عالية |
| 📐 Code Quality | 4 | مستمر |
| 🛠️ DX/DevOps | 6 | استثماري |
| **الإجمالي** | **~36** | — |

**النقاط الإيجابية في الكود:**
- ✅ نظام صلاحيات متطور (custom_permissions + RBAC + role-based)
- ✅ useApi hook ممتاز مع timeout, abort, auto-logout
- ✅ PWA support + dark mode + RTL
- ✅ ErrorBoundary موجود ويعمل
- ✅ Zod validation على المسارات الحرجة
- ✅ Graceful shutdown + healthcheck
- ✅ CI pipeline مع 3 مراحل (lint + test + build)
- ✅ نظام الترحيلات (migrations) شغال
- ✅ تأمين 95%+ من endpoints الـ API بالـ authorizePermission
- ✅ دعم متعدد الأدوار (admin, bishop, priest, supervisor, student, parent + radio roles)
