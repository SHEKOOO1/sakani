# دليل الربط بنظام SQL Server 🛡️💻

تم تجهيز النظام ليدعم الربط الكامل مع قواعد بيانات **Microsoft SQL Server**. يمكنك الآن التحول من SQLite إلى SQL Server بكل سهولة باتباع الخطوات التالية:

## ١. المتطلبات
- خادم SQL Server (محلي أو سحابي مثل Azure).
- قاعدة بيانات فارغة.

## ٢. إعداد المتغيرات البيئية
قم بفتح ملف `.env` في المسار الرئيسي للتطبيق وقم بتعديل القيم التالية:

```env
# تغيير نوع محرك قاعدة البيانات
DB_CLIENT=mssql

# بيانات الاتصال (طريقة ١: رابط مباشر)
DATABASE_URL=mssql://username:password@localhost:1433/database_name

# بيانات الاتصال (طريقة ٢: تفصيلية - بديلة للرابط)
# DB_HOST=localhost
# DB_USER=sa
# DB_PASSWORD=كلمة_السر
# DB_NAME=اسم_القاعدة
```

## ٣. تهيئة الجداول
لقد قمت بإنشاء ملف باسم `schema_mssql.sql` يحتوي على هيكل الجداول المتوافق تماماً مع SQL Server.
1. افتح **SQL Server Management Studio (SSMS)**.
2. قم بفتح ملف `schema_mssql.sql`.
3. اختر قاعدة البيانات التي أنشأتها.
4. اضغط على **Execute** لإنشاء جميع الجداول (المستخدمين، السكن، الطلاب، إلخ).

## ٤. مميزات نظام الربط الجديد (Knex.js)
- **توافقية عالية**: الكود البرمجي الآن يستخدم `Knex.js` كمحرك استعلامات، مما يعني أن الاستعلامات ستعمل على SQLite و SQL Server دون أي تعديل في الكود.
- **إدارة الاتصالات (Connection Pooling)**: يقوم النظام تلقائياً بإدارة عدد الاتصال المفتوحة مع الخادم لضمان أفضل أداء.
- **دعم المعاملات (Transactions)**: يضمن النظام سلامة البيانات عند الحذف أو الإضافة المتعددة بحيث إما أن تتم العملية بالكامل أو تُلغى بالكامل في حال حدوث خطأ.

## ملاحظة هامة:
في حال احتجت إلى أي مساعدة في ربط السيرفر الخاص بك، يرجى التأكد من أن خادم SQL Server يسمح بالاتصالات الخارجية عبر منفذ (1433).
